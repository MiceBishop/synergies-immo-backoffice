// Placeholder. Run `npm run gen:types` once the synergies-immo-services migrations
// have been applied to the remote project to overwrite this file with the real types.

export type Database = {
  public: {
    Tables: Record<string, never>
    Views: Record<string, never>
    Functions: Record<string, never>
    Enums: Record<string, never>
    CompositeTypes: Record<string, never>
  }
}
